@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager


@if (SignInManager.IsSignedIn(User))
{@* 
    <ul class="navbar-nav">  *@
    <li class="nav-item">
        <a id="manage" class="nav-link text-light" asp-area="Identity" asp-page="/Account/Manage/index" title="Manage">Hi @UserManager.GetUserName(User)!</a>
    </li>
    <li class="nav-item">
        <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button id="logout" type="submit" class="nav-link btn btn-link btn-small text-light">Logout</button>
        </form>
    </li>
    @*  </ul>  *@
}
@* else
{
     <ul>   
      <li class="nav-item">
            <a class="nav-link " id="register" style="color:white;" asp-area="Identity" asp-page="/Account/Register">Register</a>
            <a class="nav-link " id="login" style="color:white;" asp-area="Identity" asp-page="/Account/Login">Login</a> 
    </li> 
    <li class="nav-item">
        <a class="nav-link " id="login" style="color:white;" asp-area="Identity" asp-page="/Account/Login">Login</a>
    </li> 
   </ul>   
     <div class="dropdown">
        <button class="btn btn-dark dropdown-toggle" type="button" data-toggle="dropdown" aria-expanded="false">
            Register
        </button>
        <div class="dropdown-menu">
            <a class="nav-link " id="register" asp-area="Identity" asp-page="/Account/Register">Register</a>
            <a class="nav-link " id="login" asp-area="Identity" asp-page="/Account/Login">Login</a>
        </div>
    </div> 
} *@

